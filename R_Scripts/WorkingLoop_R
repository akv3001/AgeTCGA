path = "~/Users/romypichardo/Downloads/cBioportal_Survival_Data"  ## path where R must look at for the files.

out.file <-""  ## The second line creates an empty data object to store each of the imported files

file.names <- dir(path, pattern =".txt")## The third line reads the path to the files, and then a loop for reading each existing file of type “.txt” as table.

for(i in 1:length(file.names)){
  file <- read.table(file.names[i],header=FALSE, sep="\t", stringsAsFactors=FALSE)
  
  
  #############   Write a loop to iterate through the clinical files 
  #############    Create a list object with each cancer type as adataframe object
  
  
}
